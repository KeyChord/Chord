{
  "publisher": "leonsilicon",
  "name": "chordsapp-command-runner",
  "displayName": "ChordsApp Command Runner",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.cjs",
  "engines": {
    "vscode": "^1.99.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "chordsapp-command-runner.helloWorld",
        "title": "Hello World"
      }
    ],
    "configuration": {
      "title": "ChordsApp Command Runner",
      "properties": {
        "chordsapp-command-runner.message": {
          "type": "string",
          "default": "Hello World",
          "description": "The message to show in the notification"
        }
      }
    }
  },
  "scripts": {
    "build": "tsdown --env.NODE_ENV production",
    "dev": "tsdown --watch ./src --env.NODE_ENV development",
    "typecheck": "tsc --noEmit",
    "vscode:prepublish": "pnpm run build"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "@types/vscode": "^1.99.0",
    "reactive-vscode": "^1.0.0-beta.2",
    "tsdown": "^0.15.11",
    "typescript": "^5.9.3"
  }
}
