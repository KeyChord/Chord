procs:
  tauri:
    shell: bun <CONFIG_DIR>/tauri/_tauri-dev.ts
    cwd: "<CONFIG_DIR>/../../../../../../apps/chord-tauri"
    autostart: true
    stop: SIGINT
  vite:
    shell: bun run vite-dev
    cwd: "<CONFIG_DIR>/../../../../../../apps/chord-tauri"
    autostart: true
    stop: SIGINT
