import { createRootRoute } from '@chord/com.npmjs.tanstack__react-router';
import { RootLayout } from '@chord/dev.improve.chord.routes';
import '../global.css';

export const Route = createRootRoute({
	component: RootLayout,
});
